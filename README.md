# chess-game

## How to Run

The project is configured to launch the main class `ui.GuiMain` using the JavaFX Maven Plugin.

To build and run the application, open your terminal, navigate to the project root directory, and execute the following command:

```bash
mvn clean compile javafx:run